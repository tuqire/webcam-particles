const config = {
  src: 'src',
  dest: 'dest/assets',
  publicPath: 'assets',
  gitPortfolioOutput: '../../git-portfolio/webcam-particles'
};

const { src,
dest,
publicPath,
gitPortfolioOutput } = config;

module.exports = {
  src,
  dest,
  publicPath,
  gitPortfolioOutput
};
