const config = {
  src: 'src',
  dest: 'dest/assets',
  publicPath: 'assets',
  gitPortfolioOutput: '../../git-portfolio/webcam-particles'
};

const { src,
dest,
publicPath,
gitPortfolioOutput } = config;

export {
  src,
  dest,
  publicPath,
  gitPortfolioOutput
};
